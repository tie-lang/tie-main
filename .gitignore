# Rust 编译产物
/target/
**/*.rs.bk

# 示例编译产物
/examples/*.exe
/examples/*.ll
/examples/*.o
/examples/*.obj
# M5 动态库示例编译产物（dev33 批次 12）：.dll/.exp/.so 不入库（含子目录）
/examples/**/*.dll
/examples/**/*.exp
/examples/**/*.so

# IDE 与编辑器
.idea/
.vscode/
# 例外：VSCode 扩展的调试配置（F5 开发调试必需，见 editor/vscode-tie/README.md）
!editor/vscode-tie/.vscode/
!editor/vscode-tie/.vscode/*
*.swp
*.swo

# 系统与临时文件
Thumbs.db
.DS_Store
*.tmp

# 记忆与本地配置（不入库）
.localmemory/
opencode.json

# 代码索引与计划（本地生成，不入库）
.codegraph/
.omo/

# 库编译产物
/examples/*.a
/examples/*.lib
/std/*.a

# 发行版打包产物（dist/）与 REPL 自举产物（repl.exe）
/dist/
/repl/repl.exe

# 自举 v2 REPL 构建产物（T4.3）：compiler/repl.exe（tie 自写解释器 REPL，
# 由 scripts/repl-parity.ps1 / tie-llvm 编译 compiler/repl.tie 生成）与 parity
# 脚本使用的临时 Rust 通道 _rust_repl.exe（repl/repl.tie 编译到 compiler/ 下）
/compiler/repl.exe
/compiler/_rust_repl.exe

# 自举 v2 tiec 编译器产物——tiec.exe 作为 stage0 引导二进制**入库版本化**
# （自举语言惯例：删除 Rust 种子后，clone 即可用 tiec 自举；tiec 自身编译
#  compiler/driver.tie 生成，源码变更后重新自举并提交同步更新）
# /compiler/tiec.exe   ← 不再忽略（入库）

# 自举 v2 测试编译产物（tests/language/*.tie 经 tiec 编译生成）
/tests/language/*.exe

# S2.1 字符串模型探针编译产物（.tie 源文件入库，.exe 不入库）
/tests/s21_probe/*.exe

# 包管理器自举产物（pkg.exe，由 tie-llvm 编译 pkg/main.tie 生成）
/pkg/pkg.exe

# 包管理器演示项目产物（tie init 生成：main.exe 编译产物 + .tie/ 安装目录）
/examples/demo_pkg/*.exe
/examples/demo_pkg/.tie/

# 自举 v2 调试/构建残留（bench/diag/日志/中间 IR）
/compiler/*.ll
/compiler/*.exe
# tiec.exe 豁免：stage0 引导二进制入库版本化（见上方 48-51 行说明），
# 否定规则须位于 *.exe 通配之后才能生效
!/compiler/tiec.exe
/compiler/*.bc
/compiler/bench_*
/compiler/cd*.ll
/compiler/diag*
/compiler/selfhost_log*.txt
/compiler/min*.ll
/compiler/test_out.ll

# Logo 渲染临时目录（浏览器 profile 残留）
/assets/.render/
/assets/concepts/.render/
/assets/preview/.render/
# S2.3 错误处理探针编译产物（tests/s23_probe/*.tie 由 tiec 编译生成）
/tests/s23_probe/*.exe
# S2.4 接口 port 探针编译产物（tests/s24_probe/*.tie 由 tiec 编译生成）
/tests/s24_probe/*.exe
/tests/s24_probe/*.ll
# 文件系统层损坏的 tiec.exe 备份（无法删除，git 读取即崩溃 0xC0000006，忽略）
/compiler/tiec.exe.corrupt
tiec3.exe
.gitignore
probe1.exe
i128_full.exe
tiec2.exe
hello_b2.exe

# ---- p.6.8.4 Skia 裁剪 / trimmed Skia gfx ----
# 第三方源码与二进制产物不入库（源码树 + 静态库）；仅保留自写的
# 构建脚本/清单/冒烟源码/模块清单入库（白名单否定规则）。
/ext/gfx/lib/*
!/ext/gfx/lib/modules.txt
/ext/gfx/skia/*
!/ext/gfx/skia/build.tie
!/ext/gfx/skia/manifest.txt
!/ext/gfx/skia/modlist.tie
!/ext/gfx/skia/smoke/
/ext/gfx/skia/smoke/*
!/ext/gfx/skia/smoke/smoke.cpp

# ---- p.6.8.6 thunk 绑定 / extern "C" thunk bindings ----
# 仅 thunk 源码（.h/.cpp）+ 生成器 + 构建驱动入库；.obj/.exe/.png/.ll 产物
# 不入库（thunk.obj build_thunk.exe gen_thunk.exe 均在此档）。
!/ext/gfx/skia/thunk/
/ext/gfx/skia/thunk/*
!/ext/gfx/skia/thunk/thunk.h
!/ext/gfx/skia/thunk/thunk.cpp
!/ext/gfx/skia/thunk/gen_thunk.tie
!/ext/gfx/skia/thunk/build_thunk.tie
!/ext/gfx/skia/thunk/build_p687.tie

# p.6.8.6 探针编译/运行产物（.tie/.c 源入库，.exe/.ll/.obj/.png 不入库）
/tests/p686_probe/*.exe
/tests/p686_probe/*.obj
/tests/p686_probe/*.ll
/tests/p686_probe/out.png

# p.6.8.7 gfx 句柄层探针编译/运行产物（.tie 源入库，.exe/.ll/.obj/.png 不入库）
/tests/p687_probe/*.exe
/tests/p687_probe/*.obj
/tests/p687_probe/*.ll
/tests/p687_probe/out.png

# p.6.8.8 D2 命令列表探针编译/运行产物（.tie 源入库，.exe/.ll/.obj/.png 不入库）
/tests/p688_probe/*.exe
/tests/p688_probe/*.obj
/tests/p688_probe/*.ll
/tests/p688_probe/out.png

# ---- p.6.8.9 Win32 窗口嵌入层 / window embedding thunk ----
# 仅 win.h/win.cpp 源码入库；win.obj 等产物不入库（*.obj 通配 + ! 否定白名单）。
/ext/gfx/win/*
!/ext/gfx/win/win.h
!/ext/gfx/win/win.cpp

# p.6.8.9 窗口嵌入层探针编译/运行产物（.tie 源入库，.exe/.ll/.obj 不入库）
/tests/p689_probe/*.exe
/tests/p689_probe/*.obj
/tests/p689_probe/*.ll
/tests/p689_probe/out.png

# p.6.8.10 事件系统 E3 探针编译/运行产物（.tie 源入库，.exe/.ll/.obj 不入库；
#   win.obj/.exe 已在 6.8.9 *.obj 通配 + ! 白名单 覆盖，这里按目录再显式声明）
/tests/p6810_probe/*.exe
/tests/p6810_probe/*.obj
/tests/p6810_probe/*.ll

# p.6.8.11 主循环/port 探针编译/运行产物（.tie 源入库，.exe/.ll/.obj 不入库）
/tests/p6811_probe/*.exe
/tests/p6811_probe/*.obj
/tests/p6811_probe/*.ll

# p.6.8.12 组合式布局雏形探针编译/运行产物（.tie 源入库，.exe/.ll/.obj 不入库）
/tests/p6812_probe/*.exe
/tests/p6812_probe/*.obj
/tests/p6812_probe/*.ll

# p.6.8.12 全栈演示编译/运行产物（.tie 源入库，.exe/.obj/.ll 不入库）
/ext/gfx/demo/*.exe
/ext/gfx/demo/*.obj
/ext/gfx/demo/*.ll

# ---- p.6.8.13 软件光栅性能基线 thunk ----
# 仅 bench.cpp/bench.h 源码入库；bench.obj 等产物不入库。
/ext/gfx/bench/*
!/ext/gfx/bench/bench.h
!/ext/gfx/bench/bench.cpp

# p.6.8.13 性能基线探针编译/运行产物（.tie 源入库，.exe/.obj/.ll 不入库）
/tests/p6813_probe/*.exe
/tests/p6813_probe/*.obj
/tests/p6813_probe/*.ll

# p.6.8.13 验收/回归矩阵驱动编译/运行产物（.tie 源入库，.exe/.obj/.ll 不入库）
/tests/ax_matrix/*.exe
/tests/ax_matrix/*.obj
/tests/ax_matrix/*.ll
